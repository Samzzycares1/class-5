let loanAmount = 5000;
let loanInterest = 10;

let interestAmount = loanAmount * (10 / 5000);
let totalAmount = loanAmount + interestAmount;

document.write("total amount to be paid is:" + totalAmount); 